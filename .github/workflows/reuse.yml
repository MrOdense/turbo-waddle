name: Reuse other workflow
on: push
jobs: 
  call-workflow:
    uses: ./.github/workflows/reusable.yml
    with:
      who-to-greet: '@jaflo18'
  use-output: 
    runs-on: ubuntu-latest
    needs: call-workflow
    steps:
      - run: echo "Time was ${{ needs.call-workflow.outputs.current-time }}"
